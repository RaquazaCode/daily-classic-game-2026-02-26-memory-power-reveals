# Implementation Plan

1. Build deterministic core game state and card rules.
2. Add browser UI with keyboard/mouse input, scoring, pause/reset/restart.
3. Expose testing hooks (`window.advanceTime`, `window.render_game_to_text`).
4. Verify with tests, build, and Playwright captures.
5. Publish repo and update automation records.
